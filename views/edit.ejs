<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create User</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 text-white">
    
    <div class="w-full min-h-screen p-10">
        
        <div class="mb-10">
            <a class="text-blue-500 underline" href="/read">Read Users</a>
        </div>

        <h1 class="text-3xl tracking-tight mb-6">Update User</h1>

        <form action="/update/<%=user._id%>" method="post" class="flex flex-col gap-4 w-96">
            <input class="px-5 py-2 bg-transparent border-2 border-zinc-800 rounded-lg outline-none"
                   type="text" name="name" placeholder="Name" value="<%=user.name%>">

            <input class="px-5 py-2 bg-transparent border-2 border-zinc-800 rounded-lg outline-none"
                   type="text" name="email" placeholder="Email" value="<%=user.email%>">

            <input class="px-5 py-2 bg-transparent border-2 border-zinc-800 rounded-lg outline-none"
                   type="text" name="image" placeholder="Image URL" value="<%=user.image%>">

            <input type="submit"
                   class="px-5 py-2 bg-blue-500 rounded-lg cursor-pointer hover:bg-yellow-500 text-black" value="Update User">
        </form>

    </div>

</body>
</html>
